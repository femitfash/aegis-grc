import { RegisterForm } from "@/features/auth/components/RegisterForm";

export const metadata = {
  title: "Create Account - FastGRC",
  description: "Create your FastGRC account",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
